function fibonacci(n){  
	let
		var sqrt5 =  Math.sqrt(5.0)
		var fi =  (1.0 + sqrt5) / 2.0
		var psi = (1.0 - sqrt5) / 2.0
		val fib = (Math.pow(fi,n) - Math.pow(psi,n)) / sqrt5
		return fib;			
}

for(var i=0; i < 100; i++){
	console.log(fibonacci(i));
}
 