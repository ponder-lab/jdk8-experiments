package codemasters.lambda.questions;

import java.util.function.*;

public class StackOverflow {
	/*
	private static final Function<Integer, Integer> fibb = (Integer n) -> {
		if (n == 0) return 0;
		if (n == 1) return 1;
		return fibb.apply(n-2) + fibb.apply(n-1);
	};
	
	public static void main(String[] args) {
		
		Fibonacci fib = new Fibonacci();
		System.out.println(fib.get(8));
		
	}
	
	public static class Fibonacci {
		
		private Function<Integer, Integer> fibonacci = (Integer n) -> {
			if (n == 0) return 0;
			if (n == 1) return 1;
			return fibonacci.apply(n-2) + fibonacci.apply(n-1);
		};
		
		public int get(int n) {
			return fibonacci.apply(n);
		}
	}
	*/
}
